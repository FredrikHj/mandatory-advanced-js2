[/* Is showing during not the sort of movies*/]
// <helpComponents.MovieTBody
//           pasthroughMovieData={ props.movieListData }
//           pasthroughRemoveMovie={ props.removeMovie }
//         />
[/*props.movieListData.filter((obj) => {
  Is showing during the sort of movies
  obj.includes(props.movieListData);
})*/]


// countMovie += 1;
// return (
  //   <tr key={countMovie} style={(props.searchMovie != '') ? {display: 'none'}: null}><td>{ obj.title }</td><td>{ obj.director }</td><td>{ obj.rating }</td>
  //   <td><button className="deleteBtn" id={ obj.id } onClick={ props.removeMovie } value={ countMovie }>Radera filmen</button></td><td value={ countMovie }>Edit</td><td value={ countMovie }>Details</td><td>{ obj.id }</td></tr>
  // );
  //})
